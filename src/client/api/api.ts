import { UUID, SessionInfo } from '../types';

/* Method for creating users*/

// export function addUser(roleId: UUID, attributes: Attributes) {
//   return axios.post())
// }

export async function getInfo(): Promise<{ info: any }> {
  return { info: '' };
}
